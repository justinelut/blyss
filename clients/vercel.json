{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm install --no-frozen-lockfile && cd apps/web && pnpm run build",
  "outputDirectory": "apps/web/.next"
}
