name: Configure MinIO CORS

on:
  workflow_dispatch: # Manual trigger
  push:
    branches:
      - master
    paths:
      - 'oracle/redis-minio/**'
      - '.github/workflows/configure-minio.yml'

jobs:
  configure-minio:
    name: Configure MinIO CORS via Nginx
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy script to MinIO server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.MINIO_HOST }}
          username: ubuntu
          key: ${{ secrets.MINIO_SSH_KEY }}
          source: "oracle/redis-minio/scripts/configure-nginx-cors.sh"
          target: "/tmp/blyss-minio/"
          strip_components: 3

      - name: Configure MinIO CORS via Nginx
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.MINIO_HOST }}
          username: ubuntu
          key: ${{ secrets.MINIO_SSH_KEY }}
          script: |
            echo "🚀 Configuring MinIO CORS via Nginx..."

            cd /tmp/blyss-minio
            chmod +x configure-nginx-cors.sh

            # Run the configuration script
            sudo bash configure-nginx-cors.sh

            echo "✅ MinIO CORS configuration completed!"
