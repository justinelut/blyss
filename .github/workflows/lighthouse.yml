name: Web quality gates

on:
  pull_request:
    paths:
      - 'clients/web/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  design-gates:
    name: Design system gates (anti-slop · palette · primitives)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: clients/web/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
        working-directory: clients/web

      # Brand + anti-slop enforcement (plan §3.2 palette, §3.5 anti-slop,
      # §3.3/§3.4 primitives). Fails the PR on any regression.
      - name: Run design-system gates
        working-directory: clients/web
        run: >-
          pnpm vitest run
          src/__tests__/anti-slop.test.ts
          src/__tests__/forbidden-color.test.ts
          src/__tests__/design-token-compliance.test.ts
          src/design/__tests__/primitives.test.tsx

  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: clients/web/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
        working-directory: clients/web

      - run: pnpm build
        working-directory: clients/web

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v12
        with:
          configPath: clients/web/lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: Comment results on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs')
            const results = JSON.parse(fs.readFileSync('.lighthouseci/manifest.json', 'utf8'))
            const summary = results.map(r => {
              const s = r.summary
              return `| ${r.url} | ${(s.performance*100).toFixed(0)} | ${(s.accessibility*100).toFixed(0)} | ${(s.seo*100).toFixed(0)} | ${(s['best-practices']*100).toFixed(0)} |`
            }).join('\n')
            const body = `## Lighthouse Results\n| URL | Perf | A11y | SEO | BP |\n|---|---|---|---|---|\n${summary}`
            github.rest.issues.createComment({ issue_number: context.issue.number, owner: context.repo.owner, repo: context.repo.repo, body })
