========================================
POSTGRESQL STANDBY INSTANCE (Instance 3)
========================================

Deployment Date: 2026-03-19
Public IP: [Your Instance IP]
Tailscale IP: 100.81.214.7

SERVICES:
---------
PostgreSQL 16 (Standby): Port 5432 (Tailscale only)
PgBouncer: Port 6432 (Tailscale only) - RECOMMENDED
Node Exporter: Port 9100 (Tailscale only)

REPLICATION:
------------
Primary Host: 100.114.146.100
Replication Status: Streaming
Replication User: replicator
Mode: Hot Standby (Read-only)

CONNECTION STRING (READ-ONLY):
-------------------------------
Via PgBouncer (RECOMMENDED):
  postgresql://blyss:BlyssDB2024Secure!@100.81.214.7:6432/blyss

Direct PostgreSQL:
  postgresql://blyss:BlyssDB2024Secure!@100.81.214.7:5432/blyss

FOR BACKEND .ENV (READ REPLICA):
---------------------------------
POLAR_POSTGRES_READ_HOST=100.81.214.7
POLAR_POSTGRES_READ_PORT=6432
POLAR_POSTGRES_READ_USER=blyss
POLAR_POSTGRES_READ_PWD=BlyssDB2024Secure!
POLAR_POSTGRES_READ_DATABASE=blyss
POLAR_POSTGRES_READ_SSL_MODE=prefer

AUTOMATED BACKUPS:
------------------
Schedule: Daily at 2:00 AM
Retention: 7 days
Location: /var/backups/postgresql/
Format: Compressed SQL dumps (.sql.gz)

IMPORTANT COMMANDS:
-------------------
Check PostgreSQL: sudo systemctl status postgresql
Check PgBouncer: sudo systemctl status pgbouncer
Check replication: sudo -u postgres psql -c "SELECT * FROM pg_stat_wal_receiver;"

View PostgreSQL logs: sudo tail -f /var/log/postgresql/postgresql-16-main.log
View PgBouncer logs: sudo journalctl -u pgbouncer -f

Restart PostgreSQL: sudo systemctl restart postgresql
Restart PgBouncer: sudo systemctl restart pgbouncer

List backups: ls -lh /var/backups/postgresql/
Manual backup: sudo /opt/blyss/scripts/backup.sh

Check replication lag:
  sudo -u postgres psql -c "SELECT now() - pg_last_xact_replay_timestamp() AS replication_lag;"

TAILSCALE:
----------
Network: 100.x.x.x
This Instance: 100.81.214.7
Primary Instance: 100.114.146.100

FIREWALL PORTS:
---------------
22/tcp    - SSH
41641/udp - Tailscale VPN
(PostgreSQL 5432 and PgBouncer 6432 only accessible via Tailscale)

FILE LOCATIONS:
---------------
PostgreSQL Data: /var/lib/postgresql/16/main/
PostgreSQL Config: /etc/postgresql/16/main/postgresql.conf
PgBouncer Config: /etc/pgbouncer/pgbouncer.ini
Logs: /var/log/postgresql/
Backups: /var/backups/postgresql/
Backup Script: /opt/blyss/scripts/backup.sh

FAILOVER (Promote to Primary):
-------------------------------
If Primary fails, promote this standby:
  sudo -u postgres /usr/lib/postgresql/16/bin/pg_ctl promote -D /var/lib/postgresql/16/main/

Then update backend .env to use this IP as primary.

NEXT STEPS:
-----------
1. Deploy Instance 4 (Redis + MinIO)
2. Update backend .env with read replica IP
3. Test read queries from backend
4. Verify automated backups are running
5. Monitor replication lag in Grafana

NOTES:
------
- Read-only queries only (hot standby mode)
- Automatically syncs with Primary via streaming replication
- Typical replication lag: < 1 second
- Automated backups run daily at 2 AM
- Swap: 2GB at /swapfile (swappiness=10)
